

package com.fractal.config;

/*@Configuration
@EnableJpaAuditing(auditorAwareRef = "auditorAware")
public class AuditConfig {
   *//* @Bean
    public AuditorAware<User> auditorAware(AuthenticatedService authenticatedService, EntityManager entityManager) {
        return () -> {
            return Optional.of(authenticatedService.getUser());
        };
    }*//*
}*/
